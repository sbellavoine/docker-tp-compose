# Commandes

```bash
# lancer le service
docker compose up -d --build

# vérifier que le service tourne
docker compose ps

# voir les logs
docker compose logs

# arrêter et supprimer la stack
docker compose down